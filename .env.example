# TokenRun 环境变量配置
# 复制此文件为 .env 并填入真实值

# ---- Actor（昂贵模型，执行任务）----
ACTOR_API_KEY=sk-your-key-here
ACTOR_BASE_URL=https://api.openai.com/v1
ACTOR_MODEL=gpt-4o

# ---- Critic（廉价模型，审计质量）----
# 如果与 Actor 使用同一个 API，可以省略，默认继承 ACTOR_API_KEY 和 ACTOR_BASE_URL
CRITIC_API_KEY=
CRITIC_BASE_URL=
CRITIC_MODEL=gpt-4o-mini

# ---- 快捷配置：Actor 和 Critic 共用同一 API ----
# 如果两个模型都用同一个 OpenAI 兼容 API，只需设置这个：
# OPENAI_API_KEY=sk-your-key-here
# 然后删除上面的 ACTOR_API_KEY / CRITIC_API_KEY

# ------ 支持的 API 提供商 ----------------
# 支持的 API 提供商（任何 OpenAI 兼容接口）：

# OpenAI：https://api.openai.com/v1
# DeepSeek：https://api.deepseek.com/v1
# 月之暗面：https://api.moonshot.cn/v1
# 本地 Ollama：http://localhost:11434/v1
# 任何 OpenAI 兼容的代理地址